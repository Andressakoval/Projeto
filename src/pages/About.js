import React from "react";

function About() {
  return (
    <div>
      <h1> What is React JS?</h1>
      <div>
        <h2>React is a JavaScript framework created by Facebook (currently Meta) that is used to create user interfaces (UI) in web applications. </h2>
      </div>
    </div>
  );
}

export default About;
